/**
 * Style presets for email rendering.
 * 
 * Presets provide ready-to-use style configurations.
 * The `base` preset defines all default values and is automatically
 * merged as a fallback for all other presets.
 * 
 * @see ARCHITECTURE.md for how styles are applied during rendering
 */

export { base } from './base'
export { dark } from './dark'

// Font presets
export { sansSerif } from './fonts/sans-serif'
export { serif } from './fonts/serif'
export { monospace } from './fonts/monospace'
export { rounded } from './fonts/rounded'
export { humanist } from './fonts/humanist'
export { geometric } from './fonts/geometric'
